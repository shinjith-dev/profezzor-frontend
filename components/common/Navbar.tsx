import { kaushanScript } from "@/utils/fonts"

const Navbar = () => {
  return <nav>
    <div className="w-full max-w-3xl mx-auto p-5">
      <h3 className={`${kaushanScript.className} text-3xl`}>Profezzor</h3>
    </div>
  </nav>
}

export default Navbar 
